kd_type: query
kd_lambda: 1.0
alpha: 0.5              # weight for decoder cross-attention alignment term
query_matching: hungarian  # 'hungarian' (prediction-space bipartite matching,
                           # default) or 'index' (legacy first-K truncation —
                           # keep only as the ablation baseline)
