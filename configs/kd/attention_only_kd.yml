kd:
  type: feature
  lambda: 1.0
  temperature: 4  # unused for feature KD, kept for consistency
  alpha: 1.0      # only attention cosine term
  feat_weight: 0.0 # encoder MSE disabled

# Inherits from student config
student_cfg: configs/rtdetr_r18vd_coco.yml
teacher_cfg: configs/rtdetr_r50vd_coco.yml
